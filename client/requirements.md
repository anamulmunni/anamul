## Packages
ethers | Blockchain interaction (v6)
framer-motion | Smooth animations for UI elements
date-fns | Date formatting for logs

## Notes
- The app performs client-side blockchain operations using `ethers.js`.
- Successful operations trigger a backend call to `/api/log-key` to notify Telegram.
- Design uses a dark, premium aesthetic with gradients.
